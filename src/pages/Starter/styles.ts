import React from 'react';
import styled from 'styled-components';
import { Container as ContainerStrap } from 'reactstrap';

export const Container = styled(ContainerStrap)`
  max-width: 1120px;
  margin: 0 auto;
  padding: 2.5rem 1rem;
`;
export const Wrapper = styled(React.Fragment)``;
